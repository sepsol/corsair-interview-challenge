# Copy to ".env[.local|.development|.production]" file before starting the project

# Server Configuration
PORT=4000
NODE_ENV=development

# JWT Configuration
JWT_SECRET=your-secret-key-change-in-production

# Storage Configuration
# Path to directory where JSON files will be stored (relative to project root)
STORAGE_PATH=src/data/storage

# Development Settings
API_DELAY_MS=1000